package server_package.gamelogic.cards.upgrade_cards;

import server_package.gamelogic.cards.Card;
import server_package.gamelogic.cards.CardName;

public abstract class UpgradeCard extends Card {
    public UpgradeCard(CardName cardName) {
        super(cardName);
    }
}
